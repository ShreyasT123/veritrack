import type { Sighting } from "../types/telemetry";
export function SightingTicker({sightings}:{sightings:Sighting[]}) { return <section className="panel ticker"><div className="panel-title">Live camera telemetry <span>Auto-poll: 3 sec</span></div><div className="ticker-head"><span>PLATE / CLASS</span><span>CONF.</span></div><div className="ticker-scroll">{sightings.map((item,index)=><article className="sighting" key={`${item.plate}-${index}`} title={item.stream_profile}><div><b className="mono">{item.plate}</b><em>{item.vehicle_class}</em><p>{item.junction} · <span className="mono">{item.camera_id}</span></p><time>{item.timestamp}</time></div><div className="confidence"><b>{item.confidence.toFixed(1)}%</b><i><span style={{width:`${item.confidence}%`}}/></i></div></article>)}</div></section> }
