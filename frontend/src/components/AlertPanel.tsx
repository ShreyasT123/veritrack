import { AlertTriangle, FileJson, X } from "lucide-react";
import { useState } from "react";
import type { AlertRecord } from "../types/telemetry";
export function AlertPanel({alert}:{alert?:AlertRecord}) { const [open,setOpen]=useState(false); if(!alert)return <section className="panel empty">No active ERSS-112 incidents.</section>; return <><section className="alert-card"><div className="alert-heading"><AlertTriangle size={19}/><b>CRITICAL INCIDENT: CLONED VEHICLE ANOMALY</b></div><p className="velocity">{alert.implied_speed_kph.toFixed(1)} km/h <span>(Physical Teleportation Threshold: &gt; 140 km/h)</span></p><p>Identical plate <b className="mono">{alert.plate}</b> sighted at GMDA-CAM-01 (10:14:02) and GMDA-CAM-07 (10:15:32) across 18.2 km distance.</p><em>{alert.case_reference}</em><button onClick={()=>setOpen(true)}><FileJson size={15}/>View ITU-T CAP v1.2 Payload</button></section>{open&&<div className="modal-backdrop" role="presentation" onClick={()=>setOpen(false)}><section className="modal" role="dialog" aria-modal="true" onClick={e=>e.stopPropagation()}><button className="close" onClick={()=>setOpen(false)} aria-label="Close"><X/></button><h2>Emergency 112 CAD · CAP v1.2</h2><p>Restricted operational payload · {alert.identifier}</p><pre>{JSON.stringify(alert.cap,null,2)}</pre></section></div>}</> }
