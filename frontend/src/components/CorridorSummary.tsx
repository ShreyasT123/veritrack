import type { CorridorMetric } from "../types/telemetry";
import { LockKeyhole } from "lucide-react";
export function CorridorSummary({corridors}:{corridors:CorridorMetric[]}) { return <section className="panel corridors"><div className="panel-title">Corridor performance index</div>{corridors.map(c=>{const state=c.level==="CRITICAL_CONGESTION"?"critical":c.level==="ADAPTIVE_SIGNAL"?"moderate":"nominal";return <article className="corridor" key={c.corridor_id}><div><b>{c.corridor_id}</b><span className={state}>{c.level.replaceAll("_"," ")} · LOS {c.los}</span></div><div className="bar"><i className={state} style={{width:`${Math.min(100,c.cpi*100)}%`}}/></div><p><strong>{c.space_mean_speed_kph.toFixed(1)} km/h</strong> SMS · baseline {c.free_flow_speed_kph.toFixed(1)} · CPI {c.cpi.toFixed(2)}</p></article>})}<div className="ledger"><LockKeyhole size={15}/><div><b>DPDP ACT AUDIT LEDGER</b><span className="mono">Root: 4e81b75f...a3cf90d</span></div></div></section> }
